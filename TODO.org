#+TITLE: obsidian-org-mode TODO
* DONE Wire CodeMirror org helper in OrgView
  CLOSED: [2025-11-16]
  Ensured each view calls the registered orgmode helper and tears it down so folding/toggle logic actually executes.
* DONE Replace browser-only path helpers
  CLOSED: [2025-11-16]
  `_toggleImageWidget`/`_navigateLink` now use local path utilities so image/link toggles no longer throw when dirname/pathBuilder are missing.
* DONE Respect custom TODO keywords
  CLOSED: [2025-11-16]
  Updated TODO/done toggle handlers to cycle through document-defined sequences without overwriting user-defined states.
